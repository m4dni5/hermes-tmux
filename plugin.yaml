name: tmux
version: 0.4.0
description: Tmux pane observability for Hermes Agent — list, capture, send, wait
author: matt
provides_tools:
  - tmux_list
  - tmux_capture
  - tmux_send
  - tmux_wait
# No requires_env — the tools' check_fn handles availability (TMUX env + tmux binary).
# No skills shipped on purpose — see AGENTS.md for the rationale.
